name: id_verification_app
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  file_picker: ^10.2.0
  google_mlkit_text_recognition: ^0.15.0
  google_mlkit_face_detection: ^0.13.1
  shared_preferences: ^2.5.3
  path_provider: ^2.1.5
  printing: ^5.13.3
  cloud_firestore:
  sqflite_sqlcipher: ^3.4.0
  web_socket_channel:
  tflite_flutter: ^0.12.1
  flutter_secure_storage: ^9.2.4
  speech_to_text: ^7.3.0
  provider: ^6.1.5+1
  audioplayers: ^6.5.1
  sensors_plus: ^7.0.0
  video_player: ^2.9.2
  camera:
  geolocator:
  image_picker:


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:

  uses-material-design: true
sets section, like this:
  assets:
    - assets/vehicleTone.mp3
    - assets/hatchback_6469044.png
    - assets/AfzalGurustory216.jpg
